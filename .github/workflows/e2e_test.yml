name: E2E

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  e2e:
    name: E2E (atago)
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
      fail-fast: false
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10

    # POSIX runners use the bash bootstrap; the Windows runner uses the native
    # PowerShell twin (run.ps1) so the native binaries get native paths.
    defaults:
      run:
        shell: bash

    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version: "stable"

      - name: Install atago
        uses: nao1215/setup-atago@7cae89595fcbba334db0fe16fc9a24ed5b0a3fea # v0.1.2
        with:
          version: v0.22.0

      # Call the bootstrap directly (make is not present on the Windows runner).
      # Scenarios that need a POSIX shell are gated with skip: { os: windows }.
      - name: Run end-to-end tests (POSIX)
        if: runner.os != 'Windows'
        run: ./e2e/run.sh

      - name: Run end-to-end tests (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: ./e2e/run.ps1
